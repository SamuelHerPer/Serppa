import {Fragment} from 'react'

const Error = () => {
  //Página de error que se muestra cuando la url de la web no coincide con ningúna de las existentes.
  return (
    <Fragment>
    <h1>Error</h1>
    <p>No se ha encontrado esta url.</p>
    </Fragment>
  )
}

export default Error